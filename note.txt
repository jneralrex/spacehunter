Backend Changes:
1. Modified `utils/getCoordinates.js`:
   - Updated the `getCoordinates` function to return `formattedAddress` along with `lat` and `lng`.

2. Modified `controllers/house/house.controller.js`:
   - Added a new `verifyAddress` controller function that takes an address string from the request body, calls `getCoordinates`, and returns the coordinates and formatted address.
   - Modified `postHouse` to accept `lat` and `lng` from the request body as primary coordinates. If not provided, it falls back to geocoding the `location` string.
   - Modified `updateHouse` to accept `lat` and `lng` from the request body as primary coordinates for updates. If not provided, it falls back to geocoding the `location` string if the `location` field is being updated. Also added `lat` and `lng` to `allowedUpdates`.

3. Modified `routes/house/house.routes.js`:
   - Added a new `POST` route `/verify-address` that uses the `authenticate` middleware and the new `verifyAddress` controller function.

Frontend Implementation Instructions:

1.  **Create a New Component for Address Verification:**
    *   Encapsulate address input, verification, and map display logic into a reusable component.

2.  **Implement the User Interface:**
    *   Include address input fields (e.g., street, city, state, country).
    *   Add a "Verify Address" button to trigger the verification process.
    *   Provide a container for map display (using a library like Leaflet or Google Maps JavaScript API).
    *   Include a confirmation button for the user to confirm the displayed location.
    *   Use hidden input fields to store `lat` and `lng` of the verified address.

3.  **Implement the Address Verification Logic:**
    *   **"Verify Address" Button:** On click, concatenate address fields into a single string.
    *   **Call `/verify-address` Endpoint:** Make a `POST` request to `/api/verify-address` with the address string.
    *   **Display Map:** On successful response, use `lat` and `lng` to display a map with a marker at the location.
    *   **Update Hidden Input Fields:** Store the `lat` and `lng` from the response.
    *   **Handle Errors:** Display appropriate messages for API errors (e.g., address not found).

4.  **Implement User Confirmation and Pin Adjustment (Optional but Recommended):**
    *   Allow users to visually confirm the pin's location.
    *   Enable draggable markers on the map; if dragged, update the hidden `lat` and `lng` fields.

5.  **Integrate with the House Creation/Update Form:**
    *   When submitting the form to create or update a house, include the `lat` and `lng` from the hidden input fields in the payload sent to the backend.

Example Code Snippet (React/Fetch API) for Address Verification (conceptual):
```jsx
import React, { useState } from 'react';
import { MapContainer, TileLayer, Marker, useMapEvents } from 'react-leaflet';

const AddressVerification = () => {
  const [address, setAddress] = useState('');
  const [coords, setCoords] = useState(null);
  const [verifiedAddress, setVerifiedAddress] = useState('');



  const LocationMarker = () => {
    const map = useMapEvents({
      click(e) {
        setCoords(e.latlng);
        map.flyTo(e.latlng, map.getZoom());
      },
    });

    return coords === null ? null : (
      <Marker position={coords} draggable={true}></Marker>
    );
  };

  return (
    <div>
      <input
        type="text"
        value={address}
        onChange={(e) => setAddress(e.target.value)}
        placeholder="Enter address"
      />
      <button onClick={handleVerifyAddress}>Verify Address</button>

      {coords && (
        <div>
          <p>
            <b>Verified Address:</b> {verifiedAddress}
          </p>
          <MapContainer
            center={coords}
            zoom={13}
            style={{ height: '400px', width: '100%' }}
          >
            <TileLayer
              url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
            />
            <LocationMarker />
          </MapContainer>
          <input type="hidden" name="lat" value={coords.lat} />
          <input type="hidden" name="lng" value={coords.lng} />
        </div>
      )}
    </div>
  );
};

export default AddressVerification;
```


AxiosError
code
: 
"ERR_BAD_REQUEST"
config
: 
{transitional: {…}, adapter: Array(3), transformRequest: Array(1), transformResponse: Array(1), timeout: 0, …}
message
: 
"Request failed with status code 400"
name
: 
"AxiosError"
request
: 
XMLHttpRequest {onreadystatechange: null, readyState: 4, timeout: 0, withCredentials: true, upload: XMLHttpRequestUpload, …}
response
: 
config
: 
{transitional: {…}, adapter: Array(3), transformRequest: Array(1), transformResponse: Array(1), timeout: 0, …}
data
: 
error
: 
{message: 'Could not resolve coordinates for the provided address', type: 'Generic Error', details: null}
success
: 
false
[[Prototype]]
: 
Object
headers
: 
AxiosHeaders {content-length: '100', content-type: 'application/json; charset=utf-8'}
request
: 
XMLHttpRequest {onreadystatechange: null, readyState: 4, timeout: 0, withCredentials: true, upload: XMLHttpRequestUpload, …}
status
: 
400
statusText
: 
""
[[Prototype]]
: 
Object
status
: 
400
Symbol(next.console.error.digest)
: 
"NEXT_CONSOLE_ERROR"
stack
: 
"AxiosError: Request failed with status code 400\n    at settle (http://localhost:3000/_next/static/chunks/node_modules_axios_lib_99e19c7d._.js:1954:16)\n    at XMLHttpRequest.onloadend (http://localhost:3000/_next/static/chunks/node_modules_axios_lib_99e19c7d._.js:2469:174)\n    at Axios.request (http://localhost:3000/_next/static/chunks/node_modules_axios_lib_99e19c7d._.js:3273:49)\n    at async verifyAddress (http://localhost:3000/_next/static/chunks/src_ec0a35c5._.js:243:26)\n    at async handleVerifyAddress (http://localhost:3000/_next/static/chunks/src_components_AddressVerification_jsx_517fc733._.js:47:30)"
[[Prototype]]
: 
Error